// React Query cache keys — no magic strings scattered across hooks.
export const QUERY_KEYS = {
  library: ['library'] as const,
  permissions: ['permissions'] as const,
  sources: ['sources'] as const,
};
